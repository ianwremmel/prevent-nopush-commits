on: push
name: Validate
jobs:
  build:
    name: ianwremmel/prevent-nopush-commits@v1.0.0
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: ianwremmel/prevent-nopush-commits@v1.0.1
    - uses: ianwremmel/prevent-fixup-commits@v1.0.0
